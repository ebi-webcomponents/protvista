# Bring-your-own TSV file. The `.tsv` extension on `data:` infers the
# tab-separated encoding, feature records from `kind: features` — no
# `adapter:` needed, and none exists to name.
accession: P05067
rows:
  - id: MY_LAB
    label: My lab
    tracks:
      - id: hotspots
        kind: features
        data: ./hotspots.tsv
        description: Hotspot regions identified by our lab's pipeline
